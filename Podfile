# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CublicLineChart' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Charts'
  pod 'TinyConstraints'
  # Pods for CublicLineChart

  target 'CublicLineChartTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CublicLineChartUITests' do
    # Pods for testing
  end

end
